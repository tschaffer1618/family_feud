<%= form_with(model: player_season, local: true) do |form| %>
  <% if player_season.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(player_season.errors.count, "error") %> prohibited this player_season from being saved:</h2>

      <ul>
      <% player_season.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :player_id %>
    <%= form.text_field :player_id %>
  </div>

  <div class="field">
    <%= form.label :season_id %>
    <%= form.text_field :season_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
